# Retrieval Configuration for MedAssistRAG

retrieval:
  mode: "hybrid"  # Options: dense, bm25, hybrid
  dense_top_k: 20
  bm25_top_k: 20
  candidate_k: 20
  final_top_k: 5
  alpha: 0.5  # 0.5 = 50% dense + 50% BM25 keyword

  # File Paths
  faiss_index_path: "data/embeddings/faiss_index.bin"
  texts_path: "data/embeddings/texts.pkl"
  bm25_index_path: "data/embeddings/bm25_index.pkl"

  # Models
  embedding_model: "BAAI/bge-small-en"

reranker:
  enabled: true
  model_name: "cross-encoder/ms-marco-MiniLM-L-6-v2"
  top_n: 5
